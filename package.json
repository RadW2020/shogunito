{
  "name": "shogunito",
  "version": "1.0.0",
  "description": "Shogunito - Flow tracking application",
  "private": true,
  "scripts": {
    "build": "CI=true turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean",
    "migration:generate": "npm run migration:generate --workspace=shogunito-api --",
    "migration:create": "npm run migration:create --workspace=shogunito-api --",
    "migration:run": "npm run migration:run --workspace=shogunito-api",
    "migration:revert": "npm run migration:revert --workspace=shogunito-api",
    "migration:show": "npm run migration:show --workspace=shogunito-api"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.9.6",
    "checkly": "^6.9.8",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=20.19"
  },
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@types/react-window": "^1.8.8",
    "react-window": "^1.8.11",
    "react-window-infinite-loader": "^1.0.10",
    "sharp": "^0.34.5"
  }
}
